package main

func namedReturn(x int, y int) (result int) {
	result = x + y
	// return //we can use this one alone
	// We can also specify what we are returning
	return result
	// I left the two return but onlu one worked
	// Immediately I commented one, the other came up

}
